import { render, screen } from "@testing-library/react"; import { QueryClient } from "@tanstack/react-query"; import { expect, it, vi } from "vitest"; import { ApiClient } from "#/api/client"; import { Providers } from "#/app/providers"; import { WorkbenchRuntimeProvider } from "#/app/workbench-runtime-provider"; import { AuthContextProvider } from "#/auth/auth-context-provider"; import JobGroupPage from "#/features/scheduler/job-group-page"; import JobLogPage from "#/features/scheduler/job-log-page"; import JobPage from "#/features/scheduler/job-page"; import { defaultPublicFrontendConfig } from "#/runtime/public-config";
const context = { capabilities: { organizationEnabled: true, tenantEnabled: true }, menus: [], plugins: [], user: { avatar: "", email: "", homePath: "/", menus: [], permissions: ["*"], realName: "Admin", roles: [], userId: 1, username: "admin" } }; function ok(data: unknown) { return new Response(JSON.stringify({ code: 0, data }), { headers: { "content-type": "application/json" } }); } function view(page: React.ReactNode) { const fetch = vi.fn().mockImplementation(async (input: RequestInfo | URL) => String(input).includes("/job/handler") ? ok({ list: [] }) : ok({ list: [], total: 0 })); return render(<Providers queryClient={new QueryClient()}><WorkbenchRuntimeProvider value={{ apiClient: new ApiClient({ fetch }), config: defaultPublicFrontendConfig }}><AuthContextProvider value={context}>{page}</AuthContextProvider></WorkbenchRuntimeProvider></Providers>); }
it("renders all three scheduler workbenches", async () => { const group = view(<JobGroupPage />); expect(await screen.findByTestId("job-group-page")).toBeVisible(); group.unmount(); const job = view(<JobPage />); expect(await screen.findByTestId("job-page")).toBeVisible(); job.unmount(); view(<JobLogPage />); expect(await screen.findByTestId("job-log-page")).toBeVisible(); });
