import { render, screen } from "@testing-library/react"; import { QueryClient } from "@tanstack/react-query"; import { expect, it, vi } from "vitest"; import { ApiClient } from "#/api/client"; import { Providers } from "#/app/providers"; import { WorkbenchRuntimeProvider } from "#/app/workbench-runtime-provider"; import { DictTag } from "#/features/settings/dict/dict-options"; import type { PublicFrontendConfig } from "#/runtime/public-config";
it("loads and renders dictionary labels through the tenant-sensitive query family", async () => { const fetch = vi.fn().mockResolvedValue(new Response(JSON.stringify({ code: 0, data: { list: [{ cssClass: "status-ok", label: "Enabled label", tagStyle: "green", value: "1" }] } }), { headers: { "content-type": "application/json" } })); render(<Providers queryClient={new QueryClient()}><WorkbenchRuntimeProvider value={{ apiClient: new ApiClient({ fetch }), config: {} as PublicFrontendConfig }}><DictTag dictType="status" value="1" /></WorkbenchRuntimeProvider></Providers>); expect((await screen.findByText("Enabled label")).closest(".semi-tag")).toHaveClass("status-ok"); expect(fetch).toHaveBeenCalledWith("/api/v1/dict/data/type/status", expect.anything()); });
