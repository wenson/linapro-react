# Manifest Resources

`linapro-monitor-online` does not ship plugin-owned SQL tables.

It reuses the host-owned online-session truth source and only contributes menus, routes, and governance behavior.
