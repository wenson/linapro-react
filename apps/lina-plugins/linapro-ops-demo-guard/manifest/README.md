# Manifest Resources

This directory stores non-code delivery assets for `linapro-ops-demo-guard`.

## Contents

- no install or uninstall SQL is required in the current iteration because the plugin only contributes request middleware
