# 菜单管理

## Purpose

菜单管理功能负责系统菜单的创建、编辑、删除、树形展示和权限关联管理，确保前端路由、菜单权限和宿主菜单配置保持一致。
## Requirements
### Requirement:菜单列表查询

系统 SHALL 支持菜单列表的树形查询，返回所有菜单及其层级关系。

#### Scenario:查询菜单树列表
- **当** 用户访问菜单管理页面时
- **则** 系统返回菜单树列表，包括所有目录、菜单和按钮类型
- **且** 每个菜单节点包含 id、parentId、name、path、icon、type、sort、visible、status 等信息
- **且** 子菜单嵌套在父菜单的 children 字段中

#### Scenario:按条件筛选菜单
- **当** 用户输入菜单名称搜索时
- **则** 系统返回名称包含关键词的菜单列表（模糊匹配）
- **且** 返回的结果仍保持树形结构

### Requirement:菜单详情查询

系统 SHALL 支持根据菜单 ID 查询菜单详情。

#### Scenario:查询已有菜单详情
- **当** 用户点击菜单列表中的编辑按钮时
- **则** 系统返回菜单的完整信息，包括父菜单名称

#### Scenario:查询不存在的菜单
- **当** 用户请求的菜单 ID 不存在时
- **则** 系统返回错误消息"菜单不存在"

### Requirement:创建菜单

系统 SHALL 支持创建新菜单，包括目录、菜单和按钮三种类型。

#### Scenario:创建目录类型菜单
- **当** 用户填写目录信息（名称、图标、排序、是否显示、状态）并提交时
- **则** 系统创建目录类型菜单，type 为 "D"
- **且** 系统自动设置 created_at 和 updated_at
- **且** 目录类型的 path 字段用于路由分组

#### Scenario:创建菜单类型菜单
- **当** 用户填写菜单信息（名称、路由地址、组件路径、权限标识、图标、排序、是否显示、是否缓存、状态）并提交时
- **则** 系统创建菜单类型菜单，type 为 "M"
- **且** 菜单类型必须有 path 和 component 字段

#### Scenario:创建按钮类型菜单
- **当** 用户填写按钮信息（名称、权限标识、上级菜单）并提交时
- **则** 系统创建按钮类型菜单，type 为 "B"
- **且** 按钮类型没有 path、component、icon 等字段

#### Scenario:创建外链菜单
- **当** 用户创建菜单并将 is_frame 设置为 1 时
- **则** 系统将 path 视为外链地址
- **且** 前端点击该菜单时新窗口打开外链

#### Scenario:菜单名称重复
- **当** 用户创建菜单时使用已存在的菜单名称
- **则** 系统返回错误消息"菜单名称已存在"

### Requirement:更新菜单

系统 SHALL 支持更新菜单信息。

#### Scenario:更新菜单信息
- **当** 用户修改菜单信息并提交时
- **则** 系统更新菜单的 updated_at 时间戳
- **且** 系统更新菜单的所有可编辑字段

#### Scenario:更新不存在的菜单
- **当** 用户尝试更新不存在的菜单时
- **则** 系统返回错误消息"菜单不存在"

#### Scenario:编辑菜单时上级菜单选择限制
- **当** 用户编辑菜单时打开上级菜单选择器
- **则** 系统在树形列表中将当前菜单及其所有后代菜单置灰并禁用
- **且** 禁用的节点不能被选择
- **且** 新增菜单时没有禁用节点

#### Scenario:新增菜单时选择上级菜单
- **当** 用户新增菜单时打开上级菜单选择器
- **则** 所有菜单节点都可选择
- **且** 没有禁用节点

### Requirement:删除菜单

系统 SHALL 支持删除菜单并级联删除子菜单。删除过程必须在单个事务内运行；删除 `sys_role_menu` 关联的任何失败必须导致整个操作回滚，而非仅记录警告并继续删除菜单本身。

#### Scenario:删除无子菜单的菜单

- **当** 用户删除没有子菜单的菜单时
- **则** 系统在事务内软删除该菜单（设置 deleted_at）
- **且** 同步删除该菜单在 sys_role_menu 中的关联记录

#### Scenario:删除有子菜单的菜单

- **当** 用户删除有子菜单的菜单时
- **则** 系统要求级联删除确认
- **且** 用户确认后，在单个事务内删除该菜单及其所有子菜单
- **且** 同步删除所有关联的角色菜单关系

#### Scenario:关联清理失败回滚菜单删除

- **当** 用户删除菜单
- **且** `sys_role_menu` 关联清理在事务内返回错误时
- **则** 菜单软删除也必须回滚
- **且** 操作必须返回底层错误而非吞没为警告

### Requirement:获取菜单下拉树

系统 SHALL 提供菜单下拉树接口，用于角色分配菜单时的选择。

#### Scenario:获取菜单下拉树
- **当** 角色分配菜单时请求菜单树
- **则** 系统返回树形菜单列表
- **且** 过滤掉按钮类型（type="B"）的菜单
- **且** 每个节点包含 id、parentId、label、children

### Requirement:获取角色的菜单树

系统 SHALL 提供获取指定角色菜单树的接口，用于编辑角色时展示已分配的菜单。

#### Scenario:获取角色的菜单权限
- **当** 编辑角色时请求角色的菜单树
- **则** 系统返回所有菜单树
- **且** 返回该角色已分配的菜单 ID 列表（checkedKeys）
- **且** 展示菜单树时已分配的菜单被勾选

### Requirement:菜单状态控制

系统 SHALL 支持菜单启用/禁用状态与显示/隐藏切换。写入 `status` 或 `visible` 时 MUST 将该字段目标值级联到全部后代菜单。

#### Scenario:禁用菜单
- **当** 用户将菜单状态更改为禁用时
- **则** 该菜单不会出现在前端菜单栏
- **且** 通过 URL 直接访问该菜单路由时，前端应拒绝访问
- **且** 其所有后代菜单也被停用，同样不会出现在前端菜单栏

#### Scenario:启用菜单
- **当** 用户将菜单状态更改为启用时
- **则** 该菜单及其所有后代菜单同步启用
- **且** 在权限与显示条件满足时，对应菜单可重新出现在前端菜单栏

#### Scenario:隐藏菜单
- **当** 用户将菜单的 visible 字段设置为隐藏时
- **则** 该菜单不会出现在前端菜单栏
- **且** 用户仍可通过 URL 直接访问该菜单（若路由仍可访问且菜单仍启用）
- **且** 其所有后代菜单也被隐藏

#### Scenario:显示菜单
- **当** 用户将菜单的 visible 字段设置为显示时
- **则** 该菜单及其所有后代菜单同步显示
- **且** 在权限与状态条件满足时，对应菜单可重新出现在前端菜单栏

### Requirement: 菜单状态与显示双向级联

系统 SHALL 在菜单状态或显示字段被写入时，将该字段的目标值同步到其全部后代菜单。停用/隐藏与启用/显示均 MUST 级联。

#### Scenario: 停用父级目录级联停用子级

- **当** 管理员将某个菜单的状态从启用改为停用
- **且** 该菜单存在一级或多级后代菜单
- **则** 系统在同一事务内将该菜单及其所有后代菜单的 status 写为停用
- **且** 不得修改后代菜单的 visible 字段（除非本次同时请求写入 visible）

#### Scenario: 启用父级目录级联启用子级

- **当** 管理员将某个菜单的状态从停用改为启用
- **且** 该菜单存在一级或多级后代菜单
- **则** 系统在同一事务内将该菜单及其所有后代菜单的 status 写为启用
- **且** 不得修改后代菜单的 visible 字段（除非本次同时请求写入 visible）

#### Scenario: 隐藏父级目录级联隐藏子级

- **当** 管理员将某个菜单的 visible 从显示改为隐藏
- **且** 该菜单存在一级或多级后代菜单
- **则** 系统在同一事务内将该菜单及其所有后代菜单的 visible 写为隐藏
- **且** 不得修改后代菜单的 status 字段（除非本次同时请求写入 status）

#### Scenario: 显示父级目录级联显示子级

- **当** 管理员将某个菜单的 visible 从隐藏改为显示
- **且** 该菜单存在一级或多级后代菜单
- **则** 系统在同一事务内将该菜单及其所有后代菜单的 visible 写为显示
- **且** 不得修改后代菜单的 status 字段（除非本次同时请求写入 status）

#### Scenario: 无子级菜单的状态或显示变更

- **当** 管理员变更一个没有后代的菜单的状态或显示
- **则** 系统仅更新该菜单自身
- **且** 操作成功完成

### Requirement: 菜单列表支持状态与显示开关

系统 SHALL 在菜单管理列表中以开关组件展示并切换「状态」与「是否显示」，允许管理员在不打开编辑抽屉的情况下快速切换。

#### Scenario: 列表开关切换状态

- **当** 管理员在菜单列表行内点击状态开关
- **则** 系统调用菜单更新接口写入目标 status
- **且** 列表刷新后该行状态与开关状态一致
- **且** 若该菜单有后代，列表中后代状态同步为目标值

#### Scenario: 列表开关切换显示

- **当** 管理员在菜单列表行内点击显示开关
- **则** 系统调用菜单更新接口写入目标 visible
- **且** 列表刷新后该行显示状态与开关状态一致
- **且** 若该菜单有后代，列表中后代显示状态同步为目标值

#### Scenario: 开关切换后刷新可访问导航

- **当** 管理员通过列表开关成功修改当前用户可见菜单的状态或显示
- **则** 当前登录用户的左侧导航在刷新链路中立即收敛到最新结果

### Requirement:菜单管理展示插件菜单归属信息

系统 SHALL 在菜单管理能力中展示插件菜单的归属和生命周期状态，便于管理员统一管理。

#### Scenario:查看插件菜单归属

- **当** 管理员在菜单管理页面查看插件注册的菜单时
- **则** 系统能够识别该菜单所属的插件、来源类型和当前插件状态
- **且** 管理员可以区分宿主菜单和插件菜单

### Requirement:插件状态联动菜单可见性和可访问性

系统 SHALL 在插件停用、卸载或升级未生效时联合控制插件菜单的展示和访问行为。

#### Scenario:插件被禁用

- **当** 插件被禁用时
- **则** 该插件注册的菜单不会出现在前端菜单栏
- **且** 用户直接访问插件页面路由时会收到受控的不可用反馈

#### Scenario:插件重新启用

- **当** 被禁用的插件重新启用时
- **则** 原有菜单能够根据授权关系重新出现在前端菜单栏
- **且** 管理员无需重新创建菜单记录

#### Scenario:管理员修改菜单显示状态

- **当** 管理员在菜单管理页面将当前用户可见的菜单改为隐藏时
- **则** 当前登录用户的左侧导航会在该操作完成后立即移除该菜单
- **且** 如果管理员再次恢复显示，左侧导航会在同一刷新链路中立即恢复该菜单

#### Scenario:刷新期间再次发生菜单或插件状态变更

- **当** 宿主正在为当前登录用户刷新可访问菜单和动态路由，期间收到新的菜单可见性或插件状态变更时
- **则** 宿主会在当前刷新完成后立即补一轮最新状态
- **且** 左侧导航最终收敛到最新操作后的菜单结果而非停留在旧状态

### Requirement:插件管理菜单使用稳定业务标识

系统 SHALL 为插件治理的菜单提供稳定的业务标识 `menu_key`，并以此作为菜单生命周期管理的主要锚点。

#### Scenario:识别插件菜单归属

- **当** 宿主需要判断菜单属于某个插件时
- **则** 宿主优先基于 `sys_menu.menu_key` 解析菜单归属
- **且** `remark` 仅作为备注展示字段，不作为正式功能标识

#### Scenario:插件菜单安装或升级

- **当** 动态插件安装器或源码插件同步流程基于清单菜单元数据创建或更新菜单时
- **则** 宿主按 `menu_key` 执行幂等写入
- **且** 宿主根据 `parent_key` 解析真实的 `parent_id`
- **且** 相同 `menu_key` 在宿主范围内保持唯一

#### Scenario:插件卸载菜单元数据清理菜单

- **当** 动态插件卸载或宿主执行插件菜单清理时
- **则** 宿主根据清单中声明的 `menu_key` 删除对应菜单和角色关联
- **且** 宿主不依赖插件卸载 SQL 手动维护菜单删除语句

### Requirement:默认后台使用稳定的一级目录结构

系统 SHALL 为默认管理后台提供面向项目管理后台主场景的稳定一级目录结构。

#### Scenario:查询默认后台菜单骨架
- **当** 宿主为当前用户投射默认后台菜单时
- **则** 一级目录按以下结构组织：`工作台`、`权限管理`、`组织管理`、`系统设置`、`内容管理`、`系统监控`、`任务调度`、`扩展中心`、`开发中心`
- **且** 这些一级目录对应的宿主稳定父级 `menu_key` 分别为 `dashboard`、`iam`、`org`、`setting`、`content`、`monitor`、`scheduler`、`extension`、`developer`

#### Scenario:一级目录作为宿主稳定目录记录存在
- **当** 宿主初始化或同步默认后台菜单骨架时
- **则** 这些一级目录由宿主创建和拥有
- **且** 插件只能将子菜单挂载到这些目录下，而非创建新的同级一级目录

#### Scenario:默认后台扩展业务模块
- **当** 开发者持续向项目添加业务模块或官方源码插件时
- **则** 新菜单会优先放置在现有稳定目录中
- **且** 无需频繁重构一级导航命名和结构

### Requirement:插件菜单语义化挂载到宿主目录

系统 SHALL 要求插件菜单落入语义对应的宿主目录，而非统一归入插件管理目录。

#### Scenario:组织插件挂载菜单
- **当** `linapro-org-core` 将其菜单同步到宿主时
- **则** 其菜单挂载到 `组织管理`
- **且** 不挂载到 `扩展中心`

#### Scenario:内容插件挂载菜单
- **当** `linapro-content-notice` 将其菜单同步到宿主时
- **则** 其菜单挂载到 `内容管理`
- **且** 不挂载到 `扩展中心`

#### Scenario:监控插件挂载菜单
- **当** `linapro-monitor-online`、`linapro-monitor-server`、`linapro-monitor-operlog` 或 `linapro-monitor-loginlog` 将菜单同步到宿主时
- **则** 这些菜单挂载到 `系统监控`
- **且** `扩展中心/插件管理` 仍负责安装和启停管理

### Requirement:空父目录自动隐藏

系统 SHALL 在某级目录下没有可见子菜单时自动隐藏该目录，避免默认后台出现空壳导航。

#### Scenario:内容管理无可见菜单
- **当** `linapro-content-notice` 未安装、未启用或当前用户无权访问其菜单时
- **则** `内容管理` 不出现在左侧导航中

#### Scenario:部分系统监控插件缺失
- **当** 仅安装了部分监控插件且 `系统监控` 下有可见菜单时
- **则** 左侧导航仅显示可见的监控子菜单
- **且** 父目录 `系统监控` 继续保留
- **且** 如果所有监控子菜单都不可见，父目录也将被隐藏

### Requirement:菜单能力必须返回当前语言的本地化标题

系统 SHALL 在菜单树、父菜单展示、角色菜单树和动态路由投射中根据当前请求语言返回本地化菜单标题。菜单本地化必须从稳定的 `menu_key` 锚点派生翻译键。当前语言缺少翻译时，系统必须回退到默认语言或菜单默认名称。菜单编辑表单中可直接编辑的 `name` 字段必须保持数据库值，避免语言切换时将显示标题写回治理数据。

#### Scenario:菜单树返回英文标题
- **当** 用户以 `en-US` 请求菜单树或当前用户动态路由时
- **则** 响应中的菜单标题使用该语言的本地化值
- **且** 同一菜单在树节点名称和路由 `meta.title` 之间保持一致
- **且** 英文菜单标题使用简洁自然的产品措辞而非中文名称的直译

#### Scenario:资源菜单下的按钮标题使用简短动作词
- **当** 用户以 `en-US` 在菜单管理页面、父菜单树或角色菜单树中查看按钮菜单时
- **则** 资源菜单下的按钮标题使用 `Query`、`Create`、`Update`、`Delete` 和 `Export` 等简短动作词
- **且** 按钮标题避免重复父资源菜单名称，如 `Users / Create` 而非 `Users / Create User`
- **且** 无法通过通用动作清晰表达的按钮可保留 `Reset Password`、`Force Logout` 或 `Run Now` 等简短业务动作

#### Scenario:缺失的菜单翻译回退到默认名称
- **当** 菜单没有当前语言的翻译时
- **则** 系统回退到默认语言标题或菜单默认名称
- **且** 菜单结构、权限和排序不受影响

#### Scenario:管理员以英文编辑菜单
- **当** 管理员在 `en-US` 环境下打开菜单详情或编辑抽屉时
- **则** 菜单列表树、父菜单展示名称和父菜单选择器树显示当前语言的本地化标题
- **且** 表单中可编辑的 `name` 字段保持原始数据库值

### Requirement:菜单管理必须支持稳定业务键用于国际化文案

系统 SHALL 在菜单治理中保留稳定业务键作为国际化锚点，并允许宿主和插件资源通过统一翻译资源维护菜单标题，而非在多个页面硬编码相同的菜单文案。

#### Scenario:插件菜单集成国际化资源
- **当** 插件声明带有稳定 `menu_key` 的菜单并提供匹配的翻译资源时
- **则** 宿主在菜单管理页面、左侧导航和角色授权树中使用菜单的本地化标题
- **且** 管理员无需为同一插件菜单配置多个翻译映射

#### Scenario:管理员查看菜单详情
- **当** 管理员以当前语言查看菜单详情时
- **则** 系统返回当前语言的本地化父菜单展示标题和相关只读文案
- **且** 菜单的可编辑字段继续保留数据库值，稳定业务键可用于后续翻译维护和诊断

### Requirement:动态路由权限按钮必须挂载在其所属插件菜单下

从动态插件路由声明生成的按钮权限菜单 SHALL 挂载在所属动态插件页面菜单或插件根菜单下。不得在菜单管理中显示为与业务入口断开的浮动按钮集合。

#### Scenario:动态插件路由按钮是插件菜单的子节点
- **当** `linapro-demo-dynamic` 已启用且动态路由权限已同步时
- **则** 对应的按钮权限作为该插件页面菜单或插件根菜单的子节点出现
- **且** 菜单管理不显示大量以 `Dynamic Route Permission:linapro-demo-dynamic:` 为前缀的顶层或错误挂载的按钮

#### Scenario:动态插件路由按钮显示可读的英文名称
- **当** 管理员在 `en-US` 下的菜单管理中查看 `linapro-demo-dynamic` 子按钮时
- **则** 动态路由按钮名称显示可读的英文资源和动作名称
- **且** 不显示原始的 `Dynamic Route Permission:linapro-demo-dynamic:*` 占位文本

#### Scenario:动态插件按钮保持可分配
- **当** 管理员在角色授权树中查看动态插件权限时
- **则** 动态路由按钮在其所属插件菜单下保持可分配
- **且** 插件禁用通过现有插件菜单治理规则临时失效权限，不丢失授权记录

### Requirement:菜单树可展开行必须可点击

菜单管理中可展开的树形行 SHALL 显示清晰的可点击指针，并允许点击节点标题区域展开或折叠。

#### Scenario:可展开菜单行的指针和点击
- **当** 管理员悬停有子节点的目录或菜单行的标题区域时
- **则** 光标变为指针
- **且** 点击该区域展开或折叠节点

#### Scenario:移除冗余的标题图标提示
- **当** 管理员查看菜单管理列表标题区域时
- **则** 不显示标题后的额外展开提示图标
- **且** 树节点展开仍可发现和操作

### Requirement:菜单子树检查必须使用内存遍历

系统 SHALL 通过一次加载菜单父子映射并在内存中运行 BFS/DFS 来判断 `targetId` 是否属于 `parentId` 的子树（`isDescendant`）。不得在循环内为每个 `parent_id` 级别发出单独的 SQL 查询。

#### Scenario:子树检查不产生逐级 SQL 往返

- **当** 服务调用 `isDescendant(ctx, parentId, targetId)` 时
- **则** 整个检查必须最多发出一次 `dao.SysMenu` 查询来加载父子映射
- **且** 所有后续遍历必须在内存中进行

### Requirement:sys_role_menu 必须包含 menu_id 反向索引

系统 SHALL 在 `sys_role_menu` 表上维护 `KEY idx_menu_id (menu_id)`，以支持菜单级联删除时按 `menu_id` 批量删除角色菜单关联的查询路径。

#### Scenario:sys_role_menu 反向索引存在

- **当** `make db.init` 完成数据库初始化时
- **则** `SHOW INDEX FROM sys_role_menu` 必须包含列 `menu_id` 上的 `idx_menu_id`

### Requirement: 全局菜单治理写操作必须要求平台上下文

系统 SHALL 将 `sys_menu` 视为全局权限拓扑和菜单治理资源。菜单创建、更新、删除、状态变更、可见性变更以及其他会修改 `sys_menu` 或 `sys_role_menu` 全局拓扑的菜单治理写操作 MUST 要求平台上下文和对应权限字符串。租户上下文不得直接写入全局菜单模型。

#### Scenario: 租户上下文创建菜单被拒绝

- **WHEN** 租户用户具备历史或异常 `system:menu:add` 权限
- **AND** 调用菜单创建接口
- **THEN** 系统 MUST 返回结构化权限错误
- **AND** 不向 `sys_menu` 写入任何记录
- **AND** 不发布声称菜单拓扑已变更的成功响应

#### Scenario: 租户上下文更新或删除菜单被拒绝

- **WHEN** 租户用户调用菜单更新、删除、启用、禁用或显隐切换接口
- **THEN** 系统 MUST 拒绝操作
- **AND** 不修改 `sys_menu`
- **AND** 不删除或新增 `sys_role_menu` 关联

#### Scenario: 平台上下文写菜单后失效权限拓扑缓存

- **WHEN** 平台管理员在平台上下文成功创建、更新或删除菜单
- **THEN** 系统提交菜单治理变更
- **AND** 可靠发布访问拓扑缓存修订号
- **AND** 集群内其他节点在权限缓存域允许陈旧窗口内丢弃旧权限拓扑

### Requirement: 角色菜单树必须按授权上下文过滤

角色授权使用的菜单树 SHALL 按当前操作者上下文过滤为可分配集合。该过滤只影响授权展示和提交校验，不改变 `sys_menu` 的全局存储结构。

#### Scenario: 租户授权树不返回平台目录

- **WHEN** 租户管理员请求角色菜单树
- **THEN** 响应不包含平台管理目录
- **AND** 不包含平台租户管理、平台插件治理或全局菜单治理写权限节点
- **AND** 已有租户角色中异常绑定的 platform-only checked key 不得返回为可勾选授权项

#### Scenario: 平台授权树保留平台目录

- **WHEN** 平台管理员在平台上下文请求角色菜单树
- **THEN** 响应可包含平台管理目录和平台治理权限
- **AND** 仍按插件启用状态、菜单状态和既有授权展示层级规则组织树结构
