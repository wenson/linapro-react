# TapCanvas Studio 阶段执行记录模板

## 结论

用一段话说明本阶段是否通过、交付了什么、仍有哪些阻塞。不得用计划或历史结果代替当前工作区事实。

## 阶段信息

| 字段 | 值 |
| --- | --- |
| 阶段 | `<阶段编号与名称>` |
| Tasklist 版本 | `v1.0` |
| 任务范围 | `<TS-xxx 至 TS-yyy>` |
| 状态 | `In Progress / Passed / Blocked` |
| 执行日期 | `<YYYY-MM-DD>` |
| 执行分支 | `<branch>` |
| 来源基线 | `<commit 或内容摘要>` |
| 提交授权 | `<未授权 / 已授权及范围>` |

## 任务状态

| 任务 | 状态 | 证据 |
| --- | --- | --- |
| `<TS-xxx>` | `Passed / Failed / Blocked` | `<文件、命令、测试或截图>` |

## 变更范围

| 域 | 路径 | 变更 |
| --- | --- | --- |
| `<前端/插件/SQL/工具/文档>` | `<path>` | `<简述>` |

明确列出未修改的高风险边界，包括`../TapCanvas`、LinaPro认证、Tenant、RBAC、数据权限和未获授权的 Git或发布操作。

## 命令与结果

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `<可复制命令>` | `<code>` | `<通过数量、失败数量或关键输出>` |

长命令保留准确参数。OrbStack、Playwright、数据库、Go生成和构建命令分别记录，不合并成无法复现的口头结论。

## 失败与修复

| 失败 | 根因 | 修复 | 复验 |
| --- | --- | --- | --- |
| `<错误或门禁>` | `<代码事实>` | `<修改>` | `<命令与结果>` |

没有失败时写“无”，不得删除本节。

## 数据与协议门禁

- SQL幂等、软删除、自增 ID、Seed/Mock和索引结论：`<结论>`。
- DAO、DO、Entity和 Controller生成结论：`<结论>`。
- Tenant、数据权限、不可见资源和聚合不泄露结论：`<结论>`。
- API、`FlowMutation`、任务、Agent token或 tool call协议结论：`<结论>`。
- 缓存、集群、事件、租约和恢复结论：`<结论>`。

不涉及的条目标注“不涉及”并说明原因。

## 测试与视觉证据

| 门禁 | 结果 | 证据路径 |
| --- | --- | --- |
| 单元/集成 | `<passed/failed/skipped>` | `<日志或报告>` |
| E2E | `<passed/failed/skipped>` | `<报告>` |
| 构建/OrbStack | `<结果>` | `<日志>` |
| 视觉审查 | `<通过数/失败数>` | `<绝对截图路径>` |

## 影响评估

| 规则域 | 结论 | 说明 |
| --- | --- | --- |
| 架构 | `<有影响/无影响>` | `<说明>` |
| 插件 | `<有影响/无影响>` | `<说明>` |
| 前端 UI | `<有影响/无影响>` | `<说明>` |
| API | `<有影响/无影响>` | `<说明>` |
| Go与 DI | `<有影响/无影响>` | `<说明>` |
| 数据库 | `<有影响/无影响>` | `<说明>` |
| 数据权限 | `<有影响/无影响>` | `<说明>` |
| 缓存与集群 | `<有影响/无影响>` | `<说明>` |
| i18n | `<有影响/无影响>` | `<说明>` |
| 开发工具与跨平台 | `<有影响/无影响>` | `<说明>` |
| 测试 | `<有影响/无影响>` | `<说明>` |

## 审查结论

记录代码审查发现、严重级别、修复状态和剩余风险。严重问题或警告未关闭时，本阶段不得标记`Passed`。

## Git与回退边界

- 父仓跟踪状态：`<已跟踪/未跟踪及数量>`。
- commit、push、PR、tag、镜像和环境发布：`<执行情况>`。
- 原子提交候选：`<路径范围>`。
- 阶段回退点：`<基线或完整阶段版本>`。

## 阶段验收

- Tasklist阶段验收：`<原文>`。
- 验收结果：`Passed / Failed / Blocked`。
- 未完成项和下一步：`<任务 ID与原因>`。
