# React 工作台阶段执行记录模板

## 文档定位

本文定义 React 工作台替换每个阶段的统一执行记录格式。执行者在阶段开始前复制本结构，并在阶段验收前补齐任务状态、命令退出码、失败项、截图目录和影响评估。记录只证明当前工作区事实，不代替 Git commit、CI 或发布证据。

## 阶段信息

| 字段 | 值 |
| --- | --- |
| 阶段 | `<阶段编号和名称>` |
| Tasklist 版本 | `<版本>` |
| 任务范围 | `<任务 ID>` |
| 状态 | `Not Started / In Progress / Passed / Blocked` |
| 开始时间 | `<YYYY-MM-DD HH:mm TZ>` |
| 完成时间 | `<YYYY-MM-DD HH:mm TZ>` |
| 执行分支 | `<branch>` |
| 宿主基线 | `<commit>` |
| 插件基线 | `<commit 或父仓库目录基线>` |

## 范围与边界

- 目标：`<本阶段交付结果>`。
- 修改范围：`<允许修改的路径>`。
- 禁止范围：`<本阶段不得修改的路径或行为>`。
- 前置门禁：`<已满足的 GATE 或外部条件>`。

## 任务状态

| 任务 | 状态 | 证据 |
| --- | --- | --- |
| `<ID>` | `<Pending / Passed / Blocked>` | `<文件、命令或审查记录>` |

## 命令与结果

| 序号 | 命令 | 退出码 | 结果 |
| --- | --- | --- | --- |
| 1 | `<可复制命令>` | `<0 或非 0>` | `<关键输出或失败原因>` |

命令必须记录实际运行参数。使用代理镜像、离线模式、特定环境变量或替代验证时，必须说明原因和剩余风险。

## 失败项

- 当前失败：`<无，或失败命令与原因>`。
- 已解决失败：`<无，或修复方式与重跑证据>`。
- 外部阻断：`<无，或依赖的用户输入、仓库、服务和权限>`。

## 截图与人工审查

- 截图目录：`<temp/YYYYMMDD/ 或不适用>`。
- 视觉审查：`<通过、失败或不适用及原因>`。
- E2E 质量审查：`<触发或不触发及原因>`。

## 影响评估

| 规则域 | 结论 | 证据或说明 |
| --- | --- | --- |
| 架构 | `<有影响 / 无影响>` | `<边界判断>` |
| 插件 | `<有影响 / 无影响>` | `<源码插件、动态插件或无影响>` |
| 前端 UI | `<有影响 / 无影响>` | `<React、Semi、页面行为或无影响>` |
| API | `<有影响 / 无影响>` | `<契约变化或无影响>` |
| 后端与 DI | `<有影响 / 无影响>` | `<依赖 owner、创建与传递路径，或无影响>` |
| 数据库 | `<有影响 / 无影响>` | `<SQL、DAO、索引或无影响>` |
| 数据权限 | `<有影响 / 无影响>` | `<读写边界或无影响>` |
| 缓存一致性 | `<有影响 / 无影响>` | `<权威源与失效策略，或无影响>` |
| i18n | `<有影响 / 无影响>` | `<资源归属、语言与验证，或无影响>` |
| 开发工具 | `<有影响 / 无影响>` | `<Windows、Linux、macOS 影响与验证>` |
| 测试 | `<有影响 / 无影响>` | `<单测、E2E、治理验证和跳过原因>` |
| 文档 | `<有影响 / 无影响>` | `<同步文件和镜像状态>` |

## 变更文件

- 新增：`<路径或无>`。
- 修改：`<路径或无>`。
- 删除：`<路径或无>`。
- 未经授权的 Git 操作：`<无；不得执行 commit、push 或 PR>`。

## 审查结论

- 审查范围：`git status --short`、未跟踪文件展开和本阶段任务映射。
- 已读取规则：`<AGENTS.md 和命中的规则文件>`。
- 严重问题：`<数量与列表>`。
- 警告：`<数量与列表>`。
- 剩余风险：`<无或具体风险>`。

## 阶段验收

- Tasklist 阶段验收：`<原文>`。
- 验收结果：`<Passed / Blocked>`。
- Tasklist 勾选：只有实现与验证证据全部成立后才更新。
